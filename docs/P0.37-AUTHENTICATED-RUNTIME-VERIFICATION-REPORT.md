# P0.37 — Authenticated Runtime Verification & Production Closure

Date: 2026-08-09. Method: real Supabase sessions (magic-link) + Playwright against the running app + direct PostgREST calls with a real user JWT. Nothing simulated; unverifiable items are declared NOT VERIFIED.

Test identities (real rows in auth.users): `p037.staff@liora-test.local` (role `admin`), `p037.client@liora-test.local` (role `client`).

## 1. Admin runtime — VERIFIED
All 12 panel routes (`/admin`, content, services, bookings, schedule, blog, SEO, statistics, integrations, telegram, settings, activity) render the admin shell with a staff session. Bookings list showed the real record `LIO-B501-28F1 · Test P037 · 2026-08-19 14:00 · POLSKI` with filters (status, service, language, sorting, search).

## 2. Booking E2E — VERIFIED (390x844, real client session)
Service → day → time → data → summary → confirmation completed in the UI; confirmation screen: "Rezerwacja przyjęta". Persisted row:
`service_slug=konsultacja-indywidualna, status=new, language=pl, preferred_date=2026-08-19, preferred_time=14:00`, `user_id` derived from the session (not from the payload). Days/times came from `active_schedule_windows` / `booked_dates`. No console errors.

## 3. Status machine (server-side) — VERIFIED
| Attempt | Result |
|---|---|
| new → done | rejected `BOOKING_STATUS_TRANSITION_INVALID` |
| new → confirmed | accepted |
| confirmed → confirmed | rejected `BOOKING_STATUS_UNCHANGED` |
| confirmed → done | accepted |
| done → cancelled | rejected `BOOKING_STATUS_TRANSITION_INVALID` |

## 4. Authorization — VERIFIED
- Anonymous `/admin` → Kronika gate; admin server function → `Unauthorized: no active session`.
- Client role `/admin` → "Ta część pozostaje zamknięta"; `fetchAdminBookings`, `updateAdminBookingStatus`, `saveCmsContent` → `Forbidden: staff role required`.

## 5. RLS with a real client JWT — VERIFIED
Forged booking for another user → 403; self-promotion insert into `user_roles` → 403; `site_content` write → 403; `bookings`/`profiles`/`user_roles` reads scoped to own rows; `analytics_events` returns empty.

## 6. CMS — VERIFIED
Allowlist enforced (`key is not editable`), value length limit and unsupported locale rejected. Saving `landing.hero.title` (PL) changed the public `/pl` H1 immediately; `/en` unaffected; empty value restored the i18n default.

## 7. Schedule — VERIFIED
Overlapping window rejected (`SCHEDULE_OVERLAP`); inverted range rejected by validation (`toTime must be later than fromTime`).

## 8. Blog — VERIFIED
Staff create returns a `draft` record and it appears in the admin list. Test draft deleted afterwards.

## 9. Telegram — PARTIALLY VERIFIED
`/api/public/telegram/admin` and `/api/public/telegram/stats`: 401 with no secret and with a wrong secret; no bypass when unconfigured. Integrations panel reports `configured:false, transport:not_configured`. Real bot round-trip: NOT VERIFIED (no token/secret provisioned).

## 10. Analytics / GDPR — VERIFIED
Stored events contain no IP address, no user-agent string, no e-mail: only event type, locale, path, random visitor id, visitor type, coarse (currently empty) geo, device class, referrer category. Ingestion is write-only and returns `{ ok }`; aggregate reads require a staff role. Funnel reflected the real E2E run (started 4 / completed 1 / 25%).

## 11. NOT VERIFIED (declared, not simulated)
- Transactional e-mail for created / confirmed / cancelled / completed — no mail API key configured.
- Live Telegram bot delivery — no bot token / webhook secret configured.

## Status
P0.37 = GREEN for every surface reachable with the credentials available; the two transport items above remain explicitly NOT VERIFIED and require secrets.
